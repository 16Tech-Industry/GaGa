import { Component } from '@angular/core';

@Component({
  selector: 'app-temperatura',   // Nombre de la etiqueta HTML para usar este componente
  imports: [],                   // Aquí irían módulos comunes si el componente los necesitara
  templateUrl: './temperatura.html', // Archivo de plantilla asociado
  styleUrls: ['./temperatura.css']   // Archivo de estilos asociado 
})
export class Temperatura {
  // Clase del componente Temperatura
  // Por ahora está vacía, lista para agregar lógica en el futuro
}
